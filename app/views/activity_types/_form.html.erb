<%= bootstrap_form_for(@activity_type) do |f| %>
  <% if @activity_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@activity_type.errors.count, "error") %> prohibited this activity_type from being saved:</h2>

      <ul>
      <% @activity_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <form class="form-signin">
    <%= f.text_field :name %>
    <%= f.select(:color, options_for_select(options_with_colors), {}) %>
    <%= f.submit(:text =>'Create activity type', :class => "btn btn-lg btn-primary btn-block", :style => "width:auto" ) %>
  </form>  
<% end %>